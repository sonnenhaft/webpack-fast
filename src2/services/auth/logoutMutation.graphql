mutation logout($ufinityAccessToken: String!, $nagraAccessToken: String!) {
  logout(
    ufinityAccessToken: $ufinityAccessToken, 
  	nagraAccessToken: $nagraAccessToken
  ) {
    nagra
    ufinity
  }  
}
