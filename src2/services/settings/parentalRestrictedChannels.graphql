query parentalPinRestrictedChannels {
  parentalPin {
    isEnabled
    settings {
      restrictChannelsByPin
      restrictContentByPin
    }
    restrictedChannels {
      tvChannel
      id
    }
    maxRating: rating
  }
}
